url: http://localhost:8081
name: "Smoke Suite - Web"
tags:
  - smoke
  - ci
  - web
---
# Wait for app to fully load (Expo web can take a while on first bundle)
- extendedWaitUntil:
    visible: "Welcome Back"
    timeout: 30000

# S1: Student login -> verify dashboard
- tapOn: "you@email.com"
- inputText: ${E2E_STUDENT_EMAIL}
- tapOn: "Enter password"
- inputText: ${E2E_STUDENT_PASSWORD}
- tapOn: "Sign In"
- extendedWaitUntil:
    visible: "Welcome back"
    timeout: 20000
- assertVisible: "Total XP"
- assertVisible: "Active Quests"
