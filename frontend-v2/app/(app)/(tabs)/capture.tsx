/**
 * Capture - Placeholder screen. The tab bar intercepts taps on this tab
 * and opens the CaptureSheet modal instead of navigating here.
 */

import { View } from 'react-native';

export default function CaptureScreen() {
  return <View />;
}
