# ⚠ NOT INSTALLED BY ANYTHING. Render installs the ROOT requirements.txt
# (render.yaml rootDir "" + `pip install -r requirements.txt`), CI installs the
# root file, and pip-audit audits the root file. This one is a leftover of the
# pre-2026 layout and it has DIVERGED: it carries bare `>=` floors for supabase,
# openai, stripe, lxml, pillow and others where root pins exact versions, and it
# omits Pillow, WTForms and StrEnum entirely.
#
# Following CLAUDE.md's local-setup path and installing THIS file gets you a
# resolved set that CI never audits and prod never runs (AUDIT.md L1). On this
# machine that is not hypothetical: it resolved openai 2.36, stripe 15.1 and
# supabase 2.30 against prod's 1.101, 9.12 and 2.18.
#
# It is kept rather than deleted only because deleting a file two years of docs
# point at is its own kind of surprise. Add new backend dependencies to the ROOT
# file. If you install anything, install the root file.
#
# H8: pinned versions clear CVEs across Flask/Flask-Cors/Werkzeug/gunicorn +
# transport/crypto libs (requests, PyJWT, cryptography). Crypto libs pinned
# exactly (== not >=) for reproducible prod builds.
Flask==3.1.3
Werkzeug==3.1.6
Flask-Cors==6.0.2
Flask-WTF==1.2.2
gunicorn==23.0.0
python-dotenv==1.2.2
supabase>=2.0.0
requests==2.33.0
PyJWT==2.13.0
PyYAML>=6.0
cryptography==50.0.0
google-generativeai>=0.8.0
openai>=1.0.0
redis>=5.0.0
stripe>=9.0.0
beautifulsoup4>=4.12.0
bleach>=6.0.0
lxml>=5.0.0
pillow>=10.0.0
pillow_heif>=1.0.0
PyMuPDF>=1.23.0
pdfplumber>=0.10.0
pdfminer.six>=20221105
python-docx>=1.0.0
# Invoice PDFs (services/sis_invoice_pdf.py). reportlab and not weasyprint,
# which was declared here for two years, imported by nothing, and cannot import
# at all without Pango/cairo system libraries that neither this machine nor the
# Render image has. reportlab is pure Python: no system deps, and a much smaller
# memory footprint on a backend with an OOM history.
reportlab>=4.2,<5
psutil>=5.9.0
pywebpush>=2.0.0
py-vapid>=1.9.0
http_ece>=1.2.0
flasgger>=0.9.7
posthog>=3.5.0
sentry-sdk[flask]>=2.0.0
python-dateutil>=2.8.0
tqdm>=4.65.0
python-magic>=0.4.27
