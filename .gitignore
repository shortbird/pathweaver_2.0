# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
venv/
env/
ENV/
.venv
.venv-test/
pip-log.txt
pip-delete-this-directory.txt
.pytest_cache/
.mypy_cache/
.coverage
htmlcov/
.tox/
*.egg-info/
dist/
build/

# Node
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
.pnpm-store/

# Environment variables
.env
.env.branch
.env.local
.env.production
.env.development
*.env
!.env.example
!backend/.env.example
!web/.env.example

# IDE
.vscode/
.idea/
*.swp
*.swo
*.swn
*.sublime-workspace
*.sublime-project
.DS_Store
Thumbs.db

# Testing
coverage/
coverage.xml
.nyc_output/
*.lcov

# Build outputs
dist/
build/
*.egg-info/
.cache/

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Database
*.db
*.sqlite
*.sqlite3
local_db/

# Uploads (user generated content)
uploads/
backend/uploads/
static/uploads/

# Temporary files
tmp/
temp/
*.tmp
*.temp
*.bak
*.backup
*.old

# OS files
.DS_Store
Thumbs.db
desktop.ini
*.stackdump

# Security - Never commit these
*.pem
*.key
*.cert
*.crt
*.p12
secrets/
credentials/

# Application specific
backend/instance/
backend/cache/
web/dist/
web/build/
*.pyc

# Supabase
**/supabase/.branches
**/supabase/.temp

# Test outputs
test-results/
playwright-report/
# The junit reporter's output file (mobile/playwright.config.ts writes it on
# every run). mobile/playwright-report.xml was tracked until 2026-09-08 -- a
# committed record of one 2026-04-01 run with 6 failures in it, re-diffed by
# every local e2e run. CI uploads it as an artifact; git does not need it.
playwright-report.xml
cypress/videos/
cypress/screenshots/

# Test scripts with secrets (DO NOT COMMIT)
test_spark_sso.js
test_spark_webhook.js
test_token_validation.js
verify_block_evidence.js
test_evidence_api.js
setup_spark_test_data.js

# Documentation build
docs/_build/
site/

# Package manager locks (keep these)
!package-lock.json
!yarn.lock
!pnpm-lock.yaml
!Pipfile.lock
!poetry.lock

# Mobile app (Expo / React Native) — mobile/ builds for native too
*.apk
*.aab
*.ipa
*.jks

.claude/workspace/

# Per-session scratch for the repo hooks (which files a session touched).
# .claude/settings.json and .claude/hooks/ ARE tracked -- they are the gate.
.claude/state/

test_video.mp4

# Android bug-report artifacts (never commit)
mobile/bugreport-*.zip

# Marketing PII export — never commit
facebook_custom_audience.csv

# WASC accreditation source assets (licensed toolkit — reference only, not for repo)
School Communication Toolkit/
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
web/.ds-styles.css
web/.ds-brand-safelist.html

# Saved third-party registration pages used as porting reference (contain account-session artifacts)
OSH Form/

# Build/debug output that was committed once and kept coming back (HYG-01).
# web/test-output.txt was 226KB of a vitest run from a machine nobody has;
# .debug-sessions/ is a local scratchpad.
web/test-output.txt
.debug-sessions/
