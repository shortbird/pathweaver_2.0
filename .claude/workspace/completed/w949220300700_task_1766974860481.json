{
  "id": "superadmin_analytics",
  "type": "implement_fix",
  "priority": 1,
  "payload": {
    "description": "Update analytics.py to support 'superadmin' role. Replace `user_response.data.get('role') == 'admin'` with `user_response.data.get('role') in ['admin', 'superadmin']` (line 101)",
    "files": ["backend/routes/analytics.py"],
    "context": "Adding proper superadmin role differentiation. Superadmins should have same analytics access as admins."
  }
}
