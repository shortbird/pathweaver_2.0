import React from 'react'
import ClassReviewsSection from '../../components/credit-dashboard/ClassReviewsSection'

// Standalone admin page wrapper. The class-review UI lives in
// ClassReviewsSection so it can also be embedded in the Credit Review Dashboard.
const ClassReviewsPage = () => <ClassReviewsSection />

export default ClassReviewsPage
