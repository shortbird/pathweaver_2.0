/**
 * UI Component Library - Barrel Exports
 *
 * Reusable primitive components to replace 400+ duplicate className patterns
 *
 * Usage:
 * import { Modal, Alert, Card, Input } from '@/components/ui';
 */

// Modal components
export { Modal, ModalHeader, ModalBody, ModalFooter } from './Modal';
export { default as ModalOverlay } from './ModalOverlay';

// Alert component
export { Alert } from './Alert';

// Card components
export { Card, CardHeader, CardBody, CardFooter, CardTitle } from './Card';

// Input components
export { Input, Textarea, Select } from './Input';

// Form components
export { FormField, FormLabel } from './FormField';
export { FormFooter } from './FormFooter';
