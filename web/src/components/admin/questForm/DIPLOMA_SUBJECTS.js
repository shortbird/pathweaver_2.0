/**
 * Extracted from admin/QuestForm.jsx on 2026-09-04 (QF-02), then moved again on
 * 2026-09-09 to src/constants/diplomaSubjects.js when the SIS task editors
 * needed the same list. Kept as a re-export so the admin quest form's imports
 * did not have to move with it.
 */

import DIPLOMA_SUBJECTS from '../../../constants/diplomaSubjects'

export default DIPLOMA_SUBJECTS
