[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd backend && gunicorn app_wrapper:application --bind 0.0.0.0:${PORT:-8080} --workers 1 --timeout 30 --log-level info --access-logfile - --error-logfile -"