[phases.setup]
nixPkgs = ["python311", "file"]
aptPkgs = ["libmagic1", "libmagic-dev"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "cd backend && gunicorn app:app --bind 0.0.0.0:$PORT"