{
  "name": "pathweaver-root",
  "private": true,
  "workspaces": [
    "shared"
  ],
  "dependencies": {
    "@supabase/supabase-js": "^2.79.0",
    "form-data": "^4.0.6",
    "jsonwebtoken": "^9.0.2"
  },
  "overrides": {
    "jws": "^3.2.3",
    "ws": "^8.21.3"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  },
  "scripts": {
    "generate": "node shared/scripts/generate-constants.mjs",
    "generate:check": "node shared/scripts/generate-constants.mjs --check",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:report": "playwright show-report"
  }
}
